{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Publish to epic server",
            "detail": "Publishes the git project to the epic server",
            "type": "shell",
            "command": "git push && ssh -i ~/.ssh/id_rsa server@w6.cloud.acryps.com -p 2910 && cd eggsecuter && git pull"
        }
    ]
}
